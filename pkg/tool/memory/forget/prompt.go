package forget

// prompt is the tool prompt for forget.
const prompt = `# forget
Delete a fact by its fact_id. Use recall first to find the fact_id of the fact you want to remove (e.g. because it is contradicted by newer information, or is no longer accurate).

After forgetting, call remember to store the corrected fact if needed.`
